[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Summit XL"
authors = [{name = "Victor Risager", email = "vrisag20@student.aau.dk"}]
dynamic = ["version", "description"]
