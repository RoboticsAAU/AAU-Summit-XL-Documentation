Docker
======

This page describes how to build and run the Docker container for the development environment. Subsequently, it is also possible to setup the development environment using vscode's devcontainer extension.